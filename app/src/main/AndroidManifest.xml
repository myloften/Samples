<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.loften.samples">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecyclerViewTest.RecyclerViewActivity" />
        <activity android:name=".DivViewTest.Main2Activity" />
        <activity android:name=".RetrofitTest.RetrofitActivity" />
        <activity android:name=".Component.CanvasActivity" />
        <activity android:name=".CombinationImageView.CombinationImageActivity" />
        <activity android:name=".VerificationCode.VerificationActivity" />
        <activity android:name=".Callback.CallbackActivity"></activity>
    </application>

</manifest>